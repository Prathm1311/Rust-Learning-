# Rust-Learning
* Day 1:
  1. Created a Rust project with using cargo 
  2. Only used the terminal for everything like
  3. making new project with cargo
       -- cargo new {project_name}
  4. go to > src > main.rs
  5. code like
        fn main() {
           println!("Hello, World!);
         }
  6. Compilt using either cargo build >
  7. create a runnable file
  8. run the file using > cargo run
